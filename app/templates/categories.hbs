{{#link-to "categories.new" classNames="btn btn-info"}}Create a new category{{/link-to}}

{{#table-grid content=model}}
  <!-- {{#link-to "categories.edit" currentModel.id classNames="btn btn-primary"}}Edit{{/link-to}}
  <button class="btn btn-danger" {{action "delete" currentModel}}>Delete</button> -->
{{/table-grid}}

{{outlet}}
