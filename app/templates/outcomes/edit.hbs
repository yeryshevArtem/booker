{{#modal-window model=model save="edit" close="back" title="Edit outcome"}}
  <div class="row">
    <div class="col-md-4">
      <br>
      <form>
        <div class="form-group">
          {{input type="text" value=model.name classNames="form-control" placeholder="Outcome name"}}
        </div>
        <div class="form-group">
          {{input type="text" value=model.summ classNames="form-control" placeholder="Summ"}}
        </div>
        <div class="form-group">
        <select class="form-control">
          {{#each categories as |category|}}
          <option>{{category.name}}</option>
          {{/each}}
        </select>
        </div>
        <div class="form-group">
        <select class="form-control">
          {{#each wallets as |wallet|}}
          <option>{{wallet.name}}</option>
          {{/each}}
        </select>
      </div>
      </form>
    </div>
  </div>
{{/modal-window}}
